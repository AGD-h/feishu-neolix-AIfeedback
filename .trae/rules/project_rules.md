# 项目规则

本项目的完整 AI 工作指引在仓库根目录的 `AGENTS.md` 文件中。

在执行任何任务前，先完整阅读 `AGENTS.md` 并严格遵守其中的全部约定，重点包括：

1. 项目背景与仓库分工（只改自己负责的文件夹）
2. 技术约定（Python 3.11+、密钥走 .env、DeepSeek 调用方式）
3. 工单 Schema 数据契约（字段不得擅自增删改）
4. 各模块的具体要求与禁止事项
5. 使用者是编程新手，回复用中文，代码要简单可读、关键步骤加中文注释
